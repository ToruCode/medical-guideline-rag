# -----------------------------
# Version control / CI
# -----------------------------
.git/
.github/

# -----------------------------
# Python
# -----------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# -----------------------------
# Virtual Environment
# -----------------------------
.venv/
venv/

# -----------------------------
# Environment Variables
# -----------------------------
# Only .env.example is meant to be readable inside the image build
# context; a real .env (with secrets) must never be baked into a layer.
.env
.env.*
!.env.example

# -----------------------------
# IDE / OS
# -----------------------------
.vscode/
.DS_Store
Thumbs.db

# -----------------------------
# Project data and docs (not needed inside the image)
# -----------------------------
# data/ stays excluded from the build context - PDFs and vector-store
# output must never be baked into an image layer. It is bind-mounted at
# runtime instead (compose.yaml). scripts/ IS included: it is needed to
# run scripts/index_documents.py inside the container (see Dockerfile).
data/
docs/
tests/
logs/
*.log

# -----------------------------
# Local infra volumes (if ever created alongside the repo)
# -----------------------------
qdrant_storage/
postgres_data/
